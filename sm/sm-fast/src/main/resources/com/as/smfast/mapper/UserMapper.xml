<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.as.smfast.mapper.UserMapper">
    <select id="loginByUser" parameterType="java.util.Map" resultType="com.as.smfast.dao.UserDao">
        select user_id,username,password,email,mobile,status,create_time from sys_user where username = #{username};
    </select>
    <select id="searchUserByUsername" resultType="java.lang.Integer">
        select user_id from sys_user where username = #{username};
    </select>
    <insert id="registerUser" parameterType="java.util.Map">
        insert into sys_user (user_id, username, password, salt, email, mobile, status, create_user_id, create_time)
        values (null, #{username}, #{password}, #{salt}, #{email}, #{mobile}, #{status}, #{createUserId}, #{createTime})
    </insert>
</mapper>